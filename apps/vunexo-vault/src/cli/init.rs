//! `vunexo init` — no flags in V1 (`docs/vunexo-vault/cli-ux.md`).
//!
//! Kept as its own (empty) module to mirror `application-architecture.md`'s
//! "one module per top-level command" layout even though there is currently
//! nothing to configure.
